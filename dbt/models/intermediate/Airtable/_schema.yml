version: 2

models:
  - name: int_airtable_dim_adv
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
  - name: int_airtable_dim_affectation_bu_exceptions
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
  - name: int_airtable_dim_agences
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
  - name: int_airtable_dim_sites_boussole
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
  - name: int_airtable_dim_sous_financeurs
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
  - name: int_airtable_fait_ca
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
  - name: int_airtable_fait_loyers_affectation
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
  - name: int_airtable_fait_loyers_saisie
    columns:
      - name: airtable_id
        description: "Identifiant record Airtable (clé de jointure des liens)."
        data_tests:
          - unique
          - not_null
