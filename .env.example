# Modèle de configuration — copier en `.env` et remplir (le `.env` n'est PAS versionné)
# cp .env.example .env

# --- Airtable (pipeline référentiels -> 0_airtable_raw, extract_airtable.py) ---
# PAT : https://airtable.com/create/tokens (scopes data.records:read + schema.bases:read)
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=app0j1i7IH24AefxS

# --- Sofia (pipeline quotidien main.py) ---
SOFIA_PASSWORD=

# --- SI-Emploi / MySQL (pipeline quotidien main.py) ---
MYSQL_PASSWORD=
